export default {
  read (o) {
    // let fso
    // if (window.ActiveXObject) {
    //   fso = new ActiveXObject('Scripting.FileSystemObject')
    // } else {
    //   fso = new createobject('Scripting.FileSystemObject')
    // }
    // let file = fso.GetFile(o)
    return o
  }
}
